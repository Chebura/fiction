# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
    
# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3
        
      - name: Build MkDocs
        uses: Tiryoh/actions-mkdocs@v0.20.0
        with:
          configfile: my-project/mkdocs.yml
          
      - name: Push to GitHub Pages branch
        uses: ftnext/action-push-ghpages@v1.0.0
        with:
          # Path to the directory where the static assets are located.
          build_dir: ./my-project/site
          # Specify GITHUB_TOKEN in your repository.
          github_token: ${{ secrets.GITHUB_TOKEN }}
          
      - name: Sync FTP
        uses: SamKirkland/FTP-Deploy-Action@4.0.0
        with:
          server: mrald.narod.ru
          port: 21
          protocol: ftp
          username: omrald
          password: 0TyND5RC4e
          local-dir: ./site/my-project
